---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Verify that Jenkins serves homepage
    uri:
      url: "http://localhost:80"
      status_code: 200
